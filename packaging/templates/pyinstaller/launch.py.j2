"""PyInstaller entry shim for the frozen bundle. GENERATED by `trackerkeeper bake`.

A bare module that calls the configured entry point ({{ entry_point }}). The
freeze targets this file (not ``-m {{ app_slug }}``) so PyInstaller's analysis
has a concrete script to start from.
"""
{% set entry_module, entry_func = entry_point.split(":") %}
from {{ entry_module }} import {{ entry_func }}

if __name__ == "__main__":
    {{ entry_func }}()
