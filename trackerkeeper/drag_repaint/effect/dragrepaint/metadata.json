{
    "KPackageStructure": "KWin/Effect",
    "KPlugin": {
        "Id": "{{app_id}}_dragrepaint",
        "Name": "{{app_id}} — full repaint on drag",
        "Description": "Holds {{app_id}} windows under a zero-magnitude transform while they are dragged, so KWin renders them through the full-repaint path and avoids stale blur artifacts (KWin bug 455526/457727).",
        "Category": "Appearance",
        "EnabledByDefault": false,
        "License": "MIT",
        "Authors": [
            {
                "Name": "{{app_id}}"
            }
        ]
    },
    "X-Plasma-API": "javascript"
}
