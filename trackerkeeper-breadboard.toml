# The breadboard — the live maker surface. The WINDOW (`trackerkeeper-breadboard`) and
# the AI AGENT both read and write this file; your edits here are directives
# the agent re-ingests (see AGENTS.md). Git-tracked on purpose.

schema = 2
product = "tracker keeper"
goal = "One dashboard for software & firmware updates across everything you own — apps, devices, and games, every platform, sorted by what dropped today."
purpose = "tracker keeper TRACKS updates — it tells you what's new and links the changelog; it never downloads, installs, or applies them (a watchtower, not an updater). The moat IS the fragmentation: there's no single 'latest version' API, so tracker keeper BECOMES the uniform layer over a deliberately scattered world. That's the edge (nobody else assembles it) and the work (each source needs its own checker). Strategy: auto where we can (GitHub releases, Arch/AUR, Flatpak, RSS/Atom changelog feeds), manual where we can't (iOS betas, camera firmware), grow checker-by-checker. For: someone with apps/devices/games across every platform who wants one place to see what's new each day, with changelog links and notifications."
agent_request = ""

[[ingredients]]
id = "583711"
text = "Name settled: tracker keeper (slug trackerkeeper)"
done = true
by = "August"
date = "2026-07-22"

[[ingredients]]
id = "92c9a4"
text = "Thesis: the uniform layer over a no-single-API world (moat = the work)"
done = true
by = "August"
date = "2026-07-22"

[[ingredients]]
id = "41f4a4"
text = "Source strategy: auto where we can, manual where we can't"
done = true
by = "August"
date = "2026-07-22"

[[ingredients]]
id = "fe590e"
text = "Delivery targets: Linux-first (deb/AppImage/AUR); Mac/Windows kept warm"
done = true
by = "AI"
date = "2026-07-22"

[[ingredients]]
id = "ac9750"
text = "Brand: replace the placeholder logo SVG + pick an accent"
done = true
by = "agent"
date = "2026-07-26"
note = "Logo + accent shipped. Mark: refresh wheel closing on a check (X5) — gap/arrowhead bottom-right, the one quadrant the check's diagonal never crosses; single accent because a two-tone draft's near-white check vanished on light backgrounds. Default accent violet -> brand green #2fbe8a (deep #2aab7c) across settings/theme/color_tokens/icons. One SVG feeds deb/AppImage/MSIX/macOS iconset/Windows .ico/make_app_icon. Goldens re-baked."

[[ingredients]]
id = "ff911b"
text = "Seed source list (the maker's real fleet: CachyOS, KDE Plasma, Slay the Spire 2, SteamOS/Armada, iOS dev beta, Blackmagic Camera iOS, …)"
done = true
by = "AI"
date = "2026-07-22"

[[baking]]
id = "ae4777"
text = "Data model: a Tracked item (name, platform, current version, source kind, changelog URL) + storage"
done = true
priority = "now"
by = "AI"
date = "2026-07-22"

[[baking]]
id = "954e19"
text = "Source-provider interface: check() -> latest version + changelog URL; pluggable per kind"
done = true
priority = "now"
by = "AI"
date = "2026-07-22"

[[baking]]
id = "754812"
text = "First checker: GitHub releases (covers a huge slice — apps + games)"
done = true
priority = "now"
by = "AI"
date = "2026-07-22"

[[baking]]
id = "3efac6"
text = "The dashboard UI: list of items, version, released-date, sort by release date"
done = true
priority = "now"
by = "AI"
date = "2026-07-22"

[[baking]]
id = "650142"
text = "Manual-entry item (version + changelog URL you set) — the universal fallback"
done = true
priority = "now"
by = "AI"
date = "2026-07-22"

[[baking]]
id = "f7a5a3"
text = "Checker: Arch/AUR + Flatpak (the maker's Linux fleet)"
done = true
priority = "next"
by = "agent"
date = "2026-07-25"
note = "Flatpak checker via Flathub appstream API — prefers newest STABLE release (feed carries dev builds); verified live vs VLC/Steam/Krita/Discord. Arch half shipped in the MVP."

[[baking]]
id = "cc76e7"
text = "Checker: RSS/Atom changelog feeds (KDE, distros, many apps)"
done = true
priority = "next"
by = "agent"
date = "2026-07-25"
note = "generic rss provider: feed URL + optional title filter, RSS+Atom, falls back to the entry link when a feed ships empty titles (KDE). Verified live vs KDE/Godot/python.org"

[[baking]]
id = "914799"
text = "'New since you last looked' + desktop notifications (dough.notifications)"
done = true
priority = "later"
by = "agent"
date = "2026-07-25"
note = "Item.seen_version + is_new(): NEW badge + tray tooltip count, banked on hideEvent, persisted so a restart doesn't re-shout. Notifications still fire only when latest CHANGES (unseen-based would re-notify every cycle)"

[[baking]]
id = "ba86e2"
text = "Changelog links open in browser; per-item detail"
done = true
priority = "later"
by = "agent"
date = "2026-07-25"
note = "Per-item detail dialog: click a card. CheckResult.notes + Item.latest_notes, populated by github(body)/steam(contents)/appstore(releaseNotes)/flatpak(description)/rss(content:encoded) and flattened by sources.plain_notes() — plain text, never rich text, so no remote-image loads or third-party markup. Also fixed steam maxlength=1 which truncated every body to 1 char."

[[baking]]
id = "eb70cb"
text = "rig baseline goldens once the UI settles"
done = true
priority = "later"
by = "agent"
date = "2026-07-25"
note = "Goldens baked — but first fixed the rig itself: _grab_shots hardcoded A._placeholder(), so a loaf's window golden guarded dough's 'your app starts here' screen and would have passed through ANY dashboard regression. Now grabs A.content_factory()(w) (the same factory main() uses) with QStandardPaths test mode on, so persisted state is scratch and the grab is byte-identical across runs."

[[baking]]
id = "5ca9f9"
text = "GitHub repo created + pushed (public, 3-OS CI like butterPDF)"
done = true
priority = "now"
by = "AI"
date = "2026-07-22"

[[baking]]
id = "65a3f6"
text = "Checker: App Store (iOS/Mac via iTunes lookup API — id or bundle id)"
done = true
priority = "next"
by = "agent"
date = "2026-07-24"
note = "iTunes Lookup API checker + Blackmagic Camera wired to it; iOS Dev Beta stays manual (it's the OS, no store API)"

[[baking]]
id = "3ae3d9"
text = "Dashboard columns: 'how long ago' + release channel, both sortable (asc/desc)"
done = true
priority = "now"
by = "agent"
date = "2026-07-24"
note = "Relative-time column (hour precision via preserved full timestamps) + channel column (App Store/Arch/GitHub/Manual); sort bar toggles axis + direction"

[[baking]]
id = "5b4e64"
text = "Checker: CachyOS ISO (parses the mirror's dated snapshot folders per edition)"
done = true
priority = "now"
by = "agent"
date = "2026-07-24"
note = "Added http_text seam (reusable for the future RSS checker); CachyOS item wired to desktop edition, latest ISO 2026-06-28"

[[baking]]
id = "94f09b"
text = "Checker: Apple developer releases (appledev — iOS/macOS/watchOS betas + finals via RSS filter)"
done = true
priority = "now"
by = "agent"
date = "2026-07-24"
note = "iOS Developer Beta wired to 'iOS 27' → iOS 27.0 beta 4 (24A5390f)"

[[baking]]
id = "0c2e8b"
text = "Checker: Steam (game/app updates via the public news API, patchnotes-filtered)"
done = true
priority = "now"
by = "agent"
date = "2026-07-24"
note = "Slay the Spire 2 (appid 2868840) wired → v0.109.0; extracts version from patch-note title, links the store news page"

[[baking]]
id = "85172b"
text = "Dashboard grouping: user categories (iPhone/Gaming/PC), sort within each, Group toggle"
done = true
priority = "now"
by = "agent"
date = "2026-07-24"
note = "Item.group field + Category dialog field w/ autocomplete; section headers with counts; also added GameNative (utkarshdalal/GameNative)"

[[baking]]
id = "9e7b6f"
text = "Top bar: dough-matched — hamburger + settings left, smaller window buttons, header folded onto the title line"
done = true
priority = "now"
by = "agent"
date = "2026-07-24"
note = "TopBar gained a hamburger menu + left settings + add_action/insert_title_widget/add_menu_action slots; Dashboard folds its Add/Check/count/status into the titlebar line"

[[baking]]
id = "7a0499"
text = "Utility sizing (480x620 default, 300x320 floor, responsive tiers) + system tray"
done = true
priority = "now"
by = "agent"
date = "2026-07-24"
note = "density pass: type stepped to CAPTION/TINY/MICRO tokens (font-scale still applies), card padding 11->5px, list spacing 9->5, Check/Add/mini buttons pinned to the 26px chrome height"

[[baking]]
id = "770e99"
text = "Collapsible category sections (arrow toggle, persisted, collapse/expand all)"
done = true
priority = "now"
by = "agent"
date = "2026-07-24"
note = "clickable group header with ▾/▸; collapsed set persisted in settings; collapsed header still reports 'N new'; also fixed ghost-widget repaint in _render (setParent(None) before deleteLater)"

[[baking]]
id = "425bbb"
text = "Settings UI for the tray (show_tray_icon / close_to_tray toggles) + start-minimized-to-tray"
done = true
priority = "next"
by = "agent"
date = "2026-07-25"
note = "Settings TRACKING section: check interval (15min-12h/manual, re-arms live via AppBus.tracking_prefs_changed), show tray icon, close-to-tray, start-in-tray (gated on tray.will_have_tray())"

[[baking]]
id = "09daf2"
text = "Harden test_single_instance_forwarding — CI SIGABRT flake costs a re-run on most pushes"
done = true
priority = "next"
by = "agent"
date = "2026-07-25"
note = "ROOT CAUSE: conftest's _isolate_qt_windows deleteLater'd widgets but processEvents() never delivers DeferredDelete, so the whole suite's widgets were reaped at once by the first real nested QEventLoop (_spin). Fixed with sendPostedEvents(DeferredDelete); also halved suite runtime 8-10s -> 4s"

[[baking]]
id = "cb0d95"
text = "Conditional requests (ETag/If-None-Match) + optional GitHub token"
done = true
priority = "next"
by = "agent"
date = "2026-07-25"
note = "If-None-Match/If-Modified-Since + a process-local ETag cache; a GitHub 304 costs no rate-limit quota. Verified live: 200 then 304 on the same repo. Optional token via TRACKERKEEPER_GITHUB_TOKEN/GITHUB_TOKEN/GH_TOKEN, host-scoped to api.github.com only."

[[baking]]
id = "4e3124"
text = "Distinguish 'no match' from 'unreachable' in a failed check"
done = true
priority = "next"
by = "agent"
date = "2026-07-25"
note = "sources.check_with_reason() -> (result, UNREACHABLE|NOT_FOUND) by watching the network seams: no fetch attempted = bad handle, fetch returned None = source down. Card text differs per reason."

[[baking]]
id = "086ae4"
text = "Extract the Qt-free logic out of dashboard.py (704 lines)"
done = false
priority = "later"
by = "agent"
date = "2026-07-25"
note = "humanize_age/_parse_iso/_bucket_days, the sort keys and width_tier are pure functions testable without a widget"

[[baking]]
id = "9a6b97"
text = "Give Item a stable id — refresh results are keyed by name"
done = false
priority = "later"
by = "agent"
date = "2026-07-25"
note = "_RefreshWorker emits {item.name: result}; ItemDialog enforces unique names so it works, but it's conventionally safe rather than structurally safe"

[[delivery]]
id = "267380"
text = "Cut v0.1.0 once the MVP (GitHub-releases dashboard + manual items) works"
done = true
by = "agent"
date = "2026-07-25"
note = "PUBLISHED 2026-07-25 — v0.1.0 live at github.com/wolfgangwarehaus/trackerkeeper/releases/tag/v0.1.0 with all 7 artifacts. Only the PyPI leg is outstanding (see 78bb80)."

[[delivery]]
id = "0a95b7"
text = "Ship Linux-first via the deliver board (AUR + deb/AppImage)"
done = false
note = "deb + AppImage ALREADY SHIP on every release (attached to v0.1.0 and v0.1.1). The only gap is AUR, and it is EXTERNALLY BLOCKED: needs an AUR_SSH_PRIVATE_KEY secret AND the AUR reopening new-package registration (frozen since the 2026 malware wave). Not actionable work — revisit when AUR reopens."

[[delivery]]
id = "78bb80"
text = "PyPI Trusted Publisher not registered — v0.1.0's pypi leg failed invalid-publisher"
done = true
by = "agent"
date = "2026-07-25"
note = "REGISTERED by August 2026-07-25; rerun of the failed leg succeeded. trackerkeeper 0.1.0 is live on PyPI (wheel + sdist), verified by a clean-venv 'pip install trackerkeeper' — correct version, app id, and all 5 entry points."

[[delivery]]
id = "b74f0a"
text = "Cut v0.1.1 — the heartbeat release (rss + flatpak, 8 checkers)"
done = true
by = "agent"
date = "2026-07-25"
note = "PUBLISHED 2026-07-25 — v0.1.1 live on GitHub (7 artifacts) AND PyPI (wheel+sdist w/ Sigstore attestations, uploaded automatically now the publisher is registered). Verified: pip install trackerkeeper==0.1.1 in a clean venv gives 8 checkers + the heartbeat."

[[delivery]]
id = "74e280"
text = "Cut + publish v0.1.2 — the looks pass"
done = true
by = "agent"
date = "2026-07-26"
note = "PUBLISHED 2026-07-26: GitHub (7 artifacts) + PyPI (wheel+sdist, uploaded automatically). Logo + brand accent, per-item detail, fresh/past hierarchy, category sections, legible at the 300px minimum. Verified pip install trackerkeeper==0.1.2 in a clean venv."

[[improvements]]
id = "bb5d4c"
text = "Grow the checker library: store pages, more feeds, per-platform firmware"
done = false

[[improvements]]
id = "e3b360"
text = "iOS beta + camera firmware sources (the hard, manual-first tail)"
done = true
by = "agent"
date = "2026-07-24"
note = "AUTO now: camera firmware via App Store checker; iOS beta via new appledev checker (Apple developer releases RSS, filter 'iOS 27') — no longer the manual tail"

[[improvements]]
id = "adff4b"
text = "Pull dough base updates via sync_loaf and re-verify"
done = true
by = "agent"
date = "2026-07-25"
note = "Synced 36b09a3 -> f51c08b: breadboard/terminal/ui_helpers/noborder/scaffold applied clean, goldens still PASS. First had to RECLASSIFY settings_dialog/bus/rig as manual — they carried local changes while unlisted, so --apply would have silently reverted the Tracking section (-79), tracking_prefs_changed (-5) and the rig golden fix (-11). Also declared detail_dialog.py authored."
